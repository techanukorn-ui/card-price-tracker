# card-price-tracker

เว็บติดตามราคาการ์ดสะสม Pokémon/Sports ของผู้ใช้คนเดียว (ไม่มีระบบ login แบบหลายผู้ใช้) — ดู README.md สำหรับสถาปัตยกรรม/schema

## กฎการ "ดึงราคาใหม่" จาก SNKRDUNK

เมื่อผู้ใช้ขอให้ดึงราคาใหม่ (เช่น "ดึงราคาทุกใบใหม่") ให้ทำตามนี้ทุกครั้ง — ยืนยันถูกต้องแล้วโดยผู้ใช้เมื่อ 2026-08-16:

1. เปิดหน้า `snkrdunk_url` ของแต่ละการ์ดด้วยเบราว์เซอร์จริง (claude-in-chrome) ไม่ใช่ fetch/curl ตรงๆ เพราะ SNKRDUNK มีระบบกันบอท รอโหลดสัก 2 วิ แล้วอ่านข้อความหน้าเว็บ (มีลิสต์ 売買履歴 = ประวัติขายออกจริง พร้อมวันที่และเกรด)
2. **ใช้ราคาขายออกจริงเท่านั้น** ไม่ใช่ราคาตั้งขาย (出品中最安値/"~" ในกล่องราคาบนสุด) — กรองเฉพาะแถวที่ตรงเกรดของการ์ดใบนั้น (เช่น การ์ด PSA10 ก็เอาแค่แถว PSA10)
   - ถ้าเกรดเป็น `Raw` ต้องกรองตามสภาพเฉพาะของการ์ดใบนั้น (คอลัมน์ `raw_condition` = A/B/C/D) ห้ามเอาสภาพ A-D มาปนกัน
3. เอา **3 ใบล่าสุดที่ขายภายใน 7 วัน** (นับรวมวันที่ 7 พอดี) มาเฉลี่ย
   - ถ้ามีน้อยกว่า 3 ใบใน 7 วัน ก็เฉลี่ยเท่าที่มี (อาจเหลือ 1-2 ใบ)
   - ถ้าไม่มีเลยที่ขายใน 7 วัน ให้ใช้ใบล่าสุดสุด (เก่ากว่า 7 วันก็ได้) แค่ใบเดียว ไม่ต้องเฉลี่ย
4. ดึงอัตราแลกเปลี่ยน JPY→THB ล่าสุดของวันนั้น (เช่นผ่าน `https://api.frankfurter.dev/v1/latest?base=JPY&symbols=THB`) อย่าใช้ค่าเก่าที่เคย fetch ไว้
5. POST ไปที่ `/api/update-price` ด้วย `{card_id, market_price_jpy, exchange_rate}` — route นี้ไม่ต้องล็อกอิน (ยกเว้นจาก password gate ไว้แล้ว) และจะ insert แถวใหม่เสมอ ไม่เขียนทับของเก่า
6. ถ้าพลาดดึงผิดสูตรไปแล้ว ให้ลบแถวที่ผิดออกก่อน (DELETE ตรงผ่าน Supabase REST ด้วย anon key) แล้วค่อย insert ค่าที่ถูกต้องแทน — การลบแบบนี้ต้องขอผู้ใช้ยืนยันก่อนทุกครั้ง

## กฎการเติมรูปภาพ

ทุกครั้งที่ดึงราคาใหม่ ให้เช็คด้วยว่าการ์ดใบไหนยังไม่มีรูป (`image_url` และ `custom_image_url` ว่างทั้งคู่) ถ้าไม่มี ให้เปิดหน้า SNKRDUNK ของการ์ดใบนั้น แล้วดึง URL รูปหลัก (`document.querySelectorAll('img')` กรอง `src` ที่มี `size=l` — รูปแบบ `https://cdn.snkrdunk.com/upload_bg_removed/....webp?size=l`, ปกติมีรูปเดียวที่ตรงเงื่อนไขนี้ในหน้า ไม่ใช่ og:image meta tag และไม่ใช่ thumbnail `size=m` ในโซนการ์ดที่เกี่ยวข้อง) แล้ว PATCH เข้าคอลัมน์ `image_url` ของตาราง `cards` ให้เลย ไม่ต้องรอให้ผู้ใช้สั่งซ้ำ
